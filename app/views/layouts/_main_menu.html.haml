%li
  = link_to "Веб-сайт АмГУ", "http://www.amursu.ru/"
%li{:class => ("active" if params[:action] == "domains")}
  = link_to "Список доступных сайтов", root_path
%li{:class => ("active" if params[:action] == "catalog")}
  = link_to "Каталог ссылок по дисциплинам", catalog_path
- if user_signed_in?
  %li{:class => ("active" if params[:controller] == "addresses")}
    = link_to "Интернет-адреса", addresses_path
  %li{:class => ("active" if params[:controller] == "disciplines")}
    = link_to "Дисциплины", disciplines_path
  %li{:class => ("active" if params[:controller] == "sectors")}
    = link_to "Отрасли знаний", sectors_path
  %li{:class => ("active" if params[:controller] == "zones")}
    = link_to "Доменные зоны", zones_path
